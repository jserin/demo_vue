import { UnwrapNestedRefs } from 'vue';

type OlClassOptions<T> = T extends {
    styles: infer S;
} ? {
    style: S;
} & Omit<T, "styles"> : T;
export default function usePropsAsObjectProperties<T extends Record<string, unknown>>(props: T): UnwrapNestedRefs<OlClassOptions<T>>;
export {};
