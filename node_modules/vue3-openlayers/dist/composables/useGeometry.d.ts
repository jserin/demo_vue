import { ComputedRef } from 'vue';
import { Geometry } from 'ol/geom';

export default function useGeometry<T extends Geometry>(GeometryType: {
    new (...args: never[]): T;
}, props: Record<string, unknown>): ComputedRef<T>;
